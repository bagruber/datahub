import { useState, useMemo, useRef, useEffect } from "react";

/* Dein ursprünglicher Code wurde hier eingefügt.
   Falls nötig, passe die Rendering-Logik unten an. */

function App() {
  return (
    <div>
      <h1>DataHub läuft ✅</h1>
      <p>Füge hier deine UI-Komponenten ein.</p>
    </div>
  );
}

export default App;
